# Q360-PS-RC1-FINAL Manual Verification Package

> Generated by: Kimi K2.7 Coding acting as Q PS
> Session worktree: `D:/VS CODE App/Q360/.worktrees/q360-rc1-clean`
> Commit tested: `ece2596e17dcc5f307c281c52c96e1cac37be584`
> Branch: `rc/q360-rc1-clean`
> Origin/main at test time: `a76870e7b1bc93532b1469e0a138ec4fda55badf`
> Verification window: `2026-08-08T16:06Z` – `2026-08-08T16:19Z`
> Environment: Windows 11, Node.js v24.15.1, npm 11.12.1, PostgreSQL 18 local binaries

---

## 1. Repository Baseline Commands

| Step | Command | Exit | Notes |
|------|---------|------|-------|
| Locate worktree | `ls -la "D:/VS CODE App/Q360/.worktrees/"` | 0 | Confirmed `q360-rc1-clean` exists |
| Verify branch | `git rev-parse --abbrev-ref HEAD` | 0 | `rc/q360-rc1-clean` |
| HEAD commit | `git rev-parse HEAD` | 0 | `ece2596e17dcc5f307c281c52c96e1cac37be584` |
| Parent commit | `git rev-parse HEAD~1` | 0 | `d952560260c4cbb319574efa1c5a9f4453df0134` |
| Fetch origin/main | `git fetch origin main --no-tags` | 0 | Network success |
| origin/main | `git rev-parse origin/main` | 0 | `a76870e7b1bc93532b1469e0a138ec4fda55badf` |
| Ahead/behind | `git rev-list --count HEAD ^origin/main` / `origin/main ^HEAD` | 0 | 20 ahead, 0 behind |
| Git status | `git status --short` | 0 | Dirty worktree (see Section 8) |
| Git locks | `find .git -name "*lock*"` | 0 | No locks |
| Worktrees | `git worktree list --porcelain` | 0 | No competing writer on same branch |

---

## 2. Scope Proof Commands

| Step | Command | Exit | Notes |
|------|---------|------|-------|
| Changed-file list | `git diff --name-status origin/main..HEAD` | 0 | 76 files changed |
| Excluded term scan | `git diff origin/main..HEAD | grep -iE "shared_orders\|invoices\|payment_status\|signals\|evidence"` | 0 | See evidence report for interpretation |
| Absence checks | `git grep "shared_orders" / "invoices" / "signals" HEAD -- backend/src/ src/ backend/drizzle/` | 1 / 1 / 1 | `shared_orders`, `invoices`, `signals` not present |

---

## 3. Build Verification

| Component | Command | Exit | Output |
|-----------|---------|------|--------|
| Backend | `cd backend && npm run build` | 0 | `tsc` completed with no errors |
| Frontend | `npm run build` | 0 | Vite build completed, `dist/` emitted |

---

## 4. Disposable Database Setup

| Step | Command | Exit | Notes |
|------|---------|------|-------|
| Locate Postgres | `find /c/Program\ Files/PostgreSQL -name "pg_ctl.exe"` | 0 | PostgreSQL 18 at `/c/Program Files/PostgreSQL/18/bin` |
| Init cluster | `initdb -D backend/.tmp/pg_rc1 --auth=trust --no-locale --encoding=UTF8` | 0 | Fresh cluster |
| Start server | `pg_ctl -D backend/.tmp/pg_rc1 -l logfile start` | 0 | Port 15435 |
| Create DB | `createdb -h 127.0.0.1 -p 15435 q360_rc1` | 0 | Disposable only |
| Stop/cleanup | `taskkill /PID <postgres> /T /F` then `rm -rf backend/.tmp` | 0 | No production/staging data touched |

Connection string used (disposable only):
`postgresql://Lenovo@127.0.0.1:15435/q360_rc1?sslmode=disable`

---

## 5. Migration Chain Verification

| Step | Command | Exit | Notes |
|------|---------|------|-------|
| Run Wave 0 migration | `cd backend && Q360_DATABASE_ENV=staging Q360_DATABASE_NAME=q360-staging Q360_DATABASE_HOST_ALLOWLIST=127.0.0.1,localhost DATABASE_URL=<disposable> npm run db:migrate:staging` | 0 | Track A empty-database path |
| Applied migrations | `psql ... -c "SELECT id, hash, created_at FROM drizzle.__drizzle_migrations ORDER BY id;"` | 0 | 0000–0004 applied |
| Journal present | `cat backend/drizzle/meta/_journal.json` | 0 | Entries 0–4, deterministic ordering |
| Schema fingerprints | Generated by `migrate_noninteractive.ts` | 0 | Before/after fingerprints persisted in `backend/migration-manifests/` |

Migration IDs observed:
1. `51539f0fe80bae55df9d38007ffadca44000ba7c0a7a02735d79d055915a6b4d` — `0000_wave0_initial`
2. `4e43590f5a22fc7258922ea53b19a64e2de80ff828391ee7eddcaad4f96fdca4` — `0001_restaurant_partial_index_adoption`
3. `6cf1f31fed27c256e96a6089269a5dc003c689203acf2787e2d836ad5072f0a4` — `0002_sudden_lionheart`
4. `6a365e0b32a93e02f0af80d7b44aa2d130f1411ec067f34971dad598cccd2e12` — `0003_small_stone_men`
5. `032816559c0efebffa6bd141071a72e4e6fa1b845cbd5bf89b6c31d565500cce` — `0004_inventory_core_m2`

---

## 6. Readiness & Health Verification

| Endpoint | Command | HTTP | Result |
|----------|---------|------|--------|
| `/health` | `curl -s http://127.0.0.1:13001/health` | 200 | `{"name":"Q360 Backend API","version":"0.0.1","status":"running",...}` |
| `/readyz` | `curl -s http://127.0.0.1:13001/readyz` | 200 | 27 checks passed (database, journal, baseline provenance, all RC1 tables/columns) |

Port 13001 was used because 3001 was already in use by another local process.

---

## 7. Shared Module Verification Scripts

| Script | Command | Exit | Result |
|--------|---------|------|--------|
| Customers | `npm run verify:customers` | 0 | PASS — CRUD, tenant isolation, module access, archive lifecycle |
| Quotes | `npm run verify:quotes` | 0 | PASS — totals, validation, lifecycle, tenant isolation |
| Products | `npm run verify:products` | 0 | PASS — CRUD, SKU/barcode uniqueness, tenant isolation |
| Inventory Procurement | `npm run verify:inventory-procurement` | 0 | PASS — PO receive, stock movement, finance link |
| Stock Movement Service | `npm run verify:stock-movement-service` | 0 | PASS — idempotency, negative-stock rejection, tenant isolation, ledger reconciliation |
| Tenant Identity | `npm run verify:tenant-identity` | 0 | PASS — stable businessId enforced, workspace-route rejection |
| Security AuthZ | `npm run verify:security-authz` | 0 | PASS — protected fields, JWT forgery, tenant identity contracts |
| OTP | `npm run verify:otp` | 0 | PASS — request, reject, verify, session, consume |
| Founder Brief | `npm run verify:founder-brief` | 0 | PASS — read-only, owner-only, tenant isolation, audit metadata |

All scripts ran against the disposable local database and the local backend server.

---

## 8. Restaurant Regression Verification

| Flow Step | Evidence | Classification |
|-----------|----------|----------------|
| Login | OTP + security-authz passed | PASS |
| POS order creation | `POST /api/restaurant/orders` returned 201 repeatedly | PASS |
| Kitchen/KDS transition | KDS ticket moved `new` → `cooking` → `done` | PASS |
| Ready state | Order status reflected after KDS done | PASS |
| Delivered | `POST .../deliver` returned 200, table remained occupied | PASS |
| Payment | Cashier `POST .../payments` returned 201, table became available | PASS |
| Table release | `tableAfterPayment === 'available'` | PASS |
| Role authorization | Waiter/kitchen payment attempts returned **409** instead of expected **403** | **FAIL** |

Detailed failure:
- Script: `backend/src/scripts/verify_restaurant_core.ts`
- Assertion block lines 410–450
- Actual: `waiterPaymentStatus: 409`, `kitchenPaymentStatus: 409`
- Expected: `403` (role-based forbidden)
- Root-cause hypothesis: token-supplied `waiter`/`kitchen` roles are overridden by the DB user role (`admin`) in `auth.ts` → `resolveEffectiveBusinessRole`, causing the actor to pass `record_payment` authorization and then hit the order-state check before the role check. This appears to be a pre-existing test/auth interaction issue because `backend/src/routes/restaurant.ts`, `backend/src/services/restaurantDomain.ts`, and `backend/src/middleware/auth.ts` are unchanged in RC1.

---

## 9. Migration 0003 Safety Check

| Aspect | Finding |
|--------|---------|
| Migration file | `backend/drizzle/0003_small_stone_men.sql` |
| Destructive statements | `ALTER TABLE "products" DROP CONSTRAINT "products_barcode_unique"` |
| Affected table | `products` |
| Affected columns | `barcode` (DROP NOT NULL), plus additive: `description`, `sku`, `unit`, `default_price_amount_minor`, `currency`, `status`, `created_by`, `created_at`, `updated_at` |
| Data loss risk | No direct column data loss; global uniqueness guarantee removed |
| Row rewrite | Yes — adding `currency` and `status` with `DEFAULT ... NOT NULL` rewrites the table |
| Staging compatibility | Requires pre-check for duplicate barcodes per business before applying; partial unique indexes restore tenant-scoped uniqueness immediately after |
| Recovery | Restore from encrypted backup; re-apply migration after duplicate cleanup |
| Rollback | Manual: re-add global constraint, drop new columns/indexes; no automated rollback script present |

---

## 10. Known Limitations & State

1. **Dirty worktree**: At the time of testing, the branch had uncommitted changes:
   - Modified: `backend/drizzle/0003_small_stone_men.sql`
   - Modified: `backend/src/routes/inventory.ts`
   - Untracked: `backend/src/scripts/verify_rc1_readiness.ts`, `backend/src/scripts/verify_rc1_seed_journal.ts`, and five CTO-stage docs.
   These changes are **not** part of the committed HEAD and would need to be committed or removed before a clean PR.
2. **Stale manifest**: `backend/migration-manifests/Q360-PS-M6-S3F-final-manifest.json` references commit `253786c0`, not the current HEAD `ece2596e`. It does not cover migrations 0002–0004.
3. **Restaurant role-authorization test failure**: As documented in Section 8.
4. **No staging backup restore**: Rehearsal used a disposable empty database only, not a restored staging backup. The legacy path (Track B) was not exercised.
5. **Frontend runtime not exercised**: Build passed; no browser/E2E runtime validation performed.
6. **Q AI/external providers disabled**: `Q_AI_EXTERNAL_ENABLED=false`; no live provider calls made.

---

## 11. Rollback Reference

- Backend server: local process on port 13001 was terminated; no deployment occurred.
- Database: disposable PostgreSQL cluster at `backend/.tmp/pg_rc1` was terminated and removed.
- No live staging or production database was modified.
- No secrets were changed.
- Rollback of code changes: `git checkout -- backend/drizzle/0003_small_stone_men.sql backend/src/routes/inventory.ts` (do not run without review).

---

## 12. Smallest Recommended Future CI Plan

1. **Trigger**: on every push to `rc/*` branches and on PRs to `main`.
2. **Runner job matrix**:
   - `backend-build`: `cd backend && npm ci && npm run build`
   - `frontend-build`: `npm ci && npm run build`
   - `lint`: `npm run lint`
3. **Disposable DB job** (ubuntu-latest + service container `postgres:15` or `postgres:16`):
   - Start Postgres service.
   - Set `Q360_DATABASE_ENV=staging`, `Q360_DATABASE_NAME=q360-staging`, `Q360_DATABASE_HOST_ALLOWLIST=localhost`.
   - Run `npm run db:migrate:staging`.
   - Assert `drizzle.__drizzle_migrations` contains expected rows.
4. **Integration verification job** (depends on disposable DB job):
   - Start backend against the migrated disposable DB.
   - `curl /health` and `/readyz` until 200.
   - Run: `verify:customers`, `verify:quotes`, `verify:products`, `verify:inventory-procurement`, `verify:stock-movement-service`, `verify:tenant-identity`, `verify:security-authz`, `verify:otp`.
   - Capture exit codes and fail fast on non-zero.
5. **Restaurant regression job**:
   - Run `npm run verify:restaurant`.
   - Fix the waiter/kitchen role-authorization assertion or the underlying auth resolution before declaring this gate passing.
6. **Migration safety gate**:
   - Parse each new `.sql` file in `backend/drizzle/`.
   - Reject PRs containing `DROP TABLE`, `TRUNCATE`, `DELETE`, column `DROP`, or `ALTER COLUMN ... TYPE` unless explicitly labeled `destructive-approved`.
7. **Scope gate**:
   - Reject PRs adding `shared_orders`, `invoices`, standalone commercial `payment_status`, `signals`, or autonomous AI modules.
8. **Artifact retention**: keep build logs, migration manifest, and schema fingerprints for 30 days.

---

## 13. Confirmation

- No merge was performed.
- No deployment was performed.
- No live staging or production database was accessed or modified.
- `db:push` was not executed.
- No secrets were changed.
- No Commercial Core or Q Executive expansion was added.
- No architecture changes were made.
- No unrelated branches or worktrees were cleaned or deleted.
