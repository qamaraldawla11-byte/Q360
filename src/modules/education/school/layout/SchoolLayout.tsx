import { VerticalLayout } from '@/layouts/VerticalLayout';
import { schoolManifest } from '../manifest';

export const SchoolLayout = () => {
    return <VerticalLayout manifest={schoolManifest} />;
};
