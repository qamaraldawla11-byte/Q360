export const LogisticsView = () => {
    return (
        <div>
            <h1 style={{ fontSize: '24px', fontWeight: 600, marginBottom: '24px' }}>Logistics Router</h1>
            <div style={{
                padding: '40px',
                textAlign: 'center',
                background: 'var(--bg-panel)',
                borderRadius: 'var(--radius-md)',
                border: '1px dashed var(--border-subtle)',
                color: 'var(--fg-secondary)'
            }}>
                Delivery & Logistics module structure ready.
            </div>
        </div>
    );
};
