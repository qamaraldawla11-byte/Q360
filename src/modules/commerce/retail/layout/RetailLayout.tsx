import { VerticalLayout } from '@/layouts/VerticalLayout';
import { retailManifest } from '../manifest';

export const RetailLayout = () => {
    return <VerticalLayout manifest={retailManifest} />;
};
