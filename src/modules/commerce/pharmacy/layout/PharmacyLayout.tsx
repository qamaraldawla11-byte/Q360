import { VerticalLayout } from '@/layouts/VerticalLayout';
import { pharmacyManifest } from '../manifest';

export const PharmacyLayout = () => {
    return <VerticalLayout manifest={pharmacyManifest} />;
};
