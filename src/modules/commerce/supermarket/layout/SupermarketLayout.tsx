import { VerticalLayout } from '@/layouts/VerticalLayout';
import { supermarketManifest } from '../manifest';

export const SupermarketLayout = () => {
    return <VerticalLayout manifest={supermarketManifest} />;
};
