export const MerchantsView = () => {
    return (
        <div>
            <h1 style={{ fontSize: '24px', fontWeight: 600, marginBottom: '24px' }}>Merchant Management</h1>
            <div style={{
                padding: '40px',
                textAlign: 'center',
                background: 'var(--bg-panel)',
                borderRadius: 'var(--radius-md)',
                border: '1px dashed var(--border-subtle)',
                color: 'var(--fg-secondary)'
            }}>
                Merchant directory and onboarding module structure ready.
            </div>
        </div>
    );
};
